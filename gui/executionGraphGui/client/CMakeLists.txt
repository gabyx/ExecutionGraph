

find_package(Angular REQUIRED)

set(EXECUTIONGRAPH_CLIENT_TARGET "ExecutionGraphGUI-Client")
set(EXECUTIONGRAPH_CLIENT_TARGET_OUT_DIR "${CMAKE_CURRENT_BINARY_DIR}")

add_custom_target(${EXECUTIONGRAPH_CLIENT_TARGET}
                  COMMAND "${ANGULAR_CLI_EXECUTABLE}" "build" "--output-path" "${EXECUTIONGRAPH_CLIENT_TARGET_OUT_DIR}"
                  WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR})