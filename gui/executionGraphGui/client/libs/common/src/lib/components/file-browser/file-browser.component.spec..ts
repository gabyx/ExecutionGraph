import { FileBrowserComponent } from './file-browser.component';

describe('FileBrowserComponent', () => {
  it('should create an instance', () => {
    const directive = new FileBrowserComponent();
    expect(directive).toBeTruthy();
  });
});
