import { FileInputComponent } from './file-input.component';

describe('FileInputComponent', () => {
  it('should create an instance', () => {
    const directive = new FileInputComponent();
    expect(directive).toBeTruthy();
  });
});
