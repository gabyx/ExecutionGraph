import { FileBrowserDirective } from './file-browser.directive';

describe('FileBrowserDirective', () => {
  it('should create an instance', () => {
    const directive = new FileBrowserDirective();
    expect(directive).toBeTruthy();
  });
});
