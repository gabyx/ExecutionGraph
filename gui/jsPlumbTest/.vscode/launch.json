{
    "version": "0.2.0",
    "configurations": [
    {
        "type": "chrome",
        "request": "launch",
        "name": "Launch Chrome",
        "url": "file://${workspaceRoot}/index.html",
        "webRoot": "${workspaceRoot}"
    }
    ]
}